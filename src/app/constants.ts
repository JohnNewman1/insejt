export enum Views {
    MAIN_MENU,
    GOOGLE_SEARCH,
    HISTORY_LIST,
    SETTINGS
}

export enum HistoryRecordType {
    DEFAULT,
    GOOGLE_SEARCH,
}

export const DEFAULT_SETTINGS = {
	range: '24'
}